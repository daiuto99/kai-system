# buzz-eval (compose project=buzz) — data-tier env template (NO real values here).
# The real values live ONLY in the live dir /home/leo/buzz-eval/.env (chmod 600, gitignored).
# Required key NAMES (set actual values only in the live .env, never in this tracked file):
#   POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB  -> back buzz-postgres-data (approval DB)
#   MINIO_ROOT_USER / MINIO_ROOT_PASSWORD            -> own the MinIO object store
# KAI-1479 provisioning-as-code (not a secret, safe to keep here):
BUZZ_REQUIRED_BUCKETS=buzz-media
