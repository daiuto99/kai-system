# Rule #9 review — stage tagging and label-based reconciliation

- Plane review ticket: `86447132-64fc-4659-8cf9-ecfbc121ae84`
- Reviewed commits: sonicink `0e3acbe`; worker `0a2a90d`
- Reviewer: Codex (independent of the Claude/Fable builder)
- Date: 2026-07-17
- Verdict: **PASS-with-findings**

## Independent live evidence

Ran the worker-side tagger twice with `--apply`:

```text
first apply:  4 working-board issues to tag, 47 already tagged, 243 parked skipped
              tagged 4 issues
second apply: 0 working-board issues to tag, 51 already tagged, 243 parked skipped
              tagged 0 issues
```

The first run classified four new review/finding tickets created during this
review sequence; the second proves idempotence. `state_and_plan.py --reconcile`
then returned:

```text
[state_and_plan] OK: plan.json consistent with Plane (7 stages)
```

The live KAI project has 11 labels total (well below the route's `per_page=100`
limit) and all seven required names:

```text
stage:backlog, stage:core-done, stage:m-c, stage:m-e,
stage:m-f, stage:m-g, stage:ssot
```

The live `/plane/issues?include_done=false` response exposes label names and
reported:

```text
api_stage_label_counts={'stage:ssot': 26, 'stage:backlog': 19,
                        'stage:m-c': 1, 'stage:m-e': 5}
api_missing_stage_labels=0
```

A direct synthetic mapping check confirmed label-first precedence over the name
fallback: an issue named `M-E: ...` with `stage:ssot` resolves to `ssot`.

## Finding (not fixed by reviewer)

`c7152ab` committed `scripts/.syncthing.tag_stages.py.tmp`. Its SHA-256 exactly
matches `scripts/tag_stages.py` (`6d0340dce086...`), proving it is a stray
Syncthing temporary duplicate. Filed cleanup ticket
`251d11af-c21f-4254-bc40-edfb4d9a1db9`; it was not deleted during review.
