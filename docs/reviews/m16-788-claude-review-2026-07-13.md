# M1.6 / KAI-788 Claude rule-#9 review — Tier 4 query-relevant selection — 2026-07-13

**Plane issue:** KAI-788 `a4cf2c5c-5599-4d98-9639-78fbca0be7b7` "[BUG] M2 blocker: Tier 4 budget order starves query-relevant global facts" (In Progress at review start)
**Builder:** Codex, worker commit `c34baf8` ("KAI-788 a4cf2c5c: rank Tier 4 facts by query relevance")
**Reviewer:** Claude (independent; all live claims reproduced with reviewer-owned package IDs against the 29-fact production registry; nothing sourced from `/tmp` or builder transcripts)

## VERDICT: PASS (C1–C3, C5, C6, C7 PASS; C4 interim ceiling documented; C0 standing reported)

---

## Preamble — authoritative repo state

- `/home/leo/kai-system` tree **clean**; `c34baf8` is **HEAD exactly** (`merge-base --is-ancestor` true).
- Diff stat of `c34baf8`: `kai-orchestrator/context_service.py` (+59/−6) and `kai-orchestrator/tests/test_tier4_relevance.py` (new, 86 lines) — nothing else.
- Deployed-code consistency: served `/app/context_service.py` md5-identical (`d12917e56a711f9256fb176ec8dc2dd3`) to repo `context_service.py` at HEAD.

## C0 — STANDING — PASS (with expected staleness noted)

Mac `~/sonicink` tree clean at `143e317`. Sprint_History/StateOfTheUnion record M0 review Done, M1 review Done, the CONTEXT_SPEC deviation commit, the token rotation (KAI-780 OPEN LEO ACTION closed), and KAI-782/783/784 filings. **M1.5 and M2 are not yet in the close narrative — expected, not a defect: no close has run since M1.5 by design (M1.5/M2/M1.6 close together after M2 acceptance).**

## C1 — PLANE — PASS

- KAI-788 In Progress (state `edeeec1f…`), real parent `532c0d4a…` (S7-1, D9).
- Three comments, all read from the Plane API:
  - `13fe1966` (23:11Z) — discovery from KAI-786 production acceptance: same first seven facts + `excluded_stale=19` regardless of query; explicitly forbids fixture-reorder/timestamp workarounds.
  - `d36aaa83` (23:45Z) — **scope report before code**: names the starvation mechanism with file:line, documents that **no fact vector/index exists to reuse** (facts are not embedded at write time), and records the chosen **deterministic lexical interim** as the permitted approach. The semantic-ranking deferral is on the record, not silently dropped. ✔
  - `4a00a2b6` (23:50Z) — builder evidence matching the shipped diff.

## C2 — THE FIX REPRODUCED — PASS (reviewer package IDs)

Live `POST /context/assemble` against the production 29-fact registry (SHA `195331d189386bcbc23a44367c755231a1a2dd4881ee2030f85295e73f9af8e3` verified before and after all reviewer calls — byte-identical, registry untouched):

- **a. Sky DAW/front-end query** ("Which DAW does Leo run and what does his 500-series front end preamp chain look like?") — package `1b01a504-2ace-402f-b94d-35ee89b73971`: t4 facts led by **`leo-500-series-chain-001` and `leo-primary-daw-001`** — both present, neither excluded. The exact M2 acceptance-(b) failure (builder discovery package `d9da9358` showed both absent) now passes on the reviewer's own hands. ✔
- **b. Roads main-guitar query** ("What is Leo main number one guitar?") — package `510adfa1-bb40-4db9-804d-aa6fe8286442`: **`leo-number-one-guitar-001` ranked first**. No regression. ✔

## C3 — QUERY-SENSITIVITY — PASS

Same advisor (sky), two different queries, two different t4 sets:

| Query | package_id | t4 head |
|---|---|---|
| DAW/500-series | `1b01a504…` | `leo-500-series-chain-001`, `leo-primary-daw-001`, `leo-monitor-control-001`, … |
| "What monitors does Leo mix on and how does he switch monitoring?" | `d41aedb7-d352-4627-82e0-c87e49a9f04f` | `leo-monitor-control-001`, `leo-reference-mixes-001`, … |

Sets differ materially (10 vs 7 facts, different membership and order). Selection is query-driven, not fixed. ✔

## C4 — HONEST BOUNDING OF THE INTERIM (truth-in-labeling, not a FAIL gate)

**What the ranking actually is (code read):** pure lexical token overlap. `context_service.py:62-63` — tokens are lowercase `[a-z0-9]+` runs minus a ~23-word stop list; `:293-304` `_tier4_relevance` scores `4×|query∩key| + 2×|query∩domain| + 1×|query∩value|`; `:343-349` four stable sorts (ID asc → updated_at desc → scope specificity desc → relevance desc, final sort primary); budget applied after ranking. No stemming, no synonyms, no embeddings.

**Ceiling demonstrated live:** roads query **"Which axe does he reach for first when tracking?"** — package `a07fe323-819a-429c-9833-8df1c05318c6`. The relevant fact `leo-number-one-guitar-001` does **NOT** surface: no query token matches its key/domain/value ("axe", "tracking", "reach" share no literal vocabulary with "number one guitar / Silhouette"). All facts score 0, so ranking degrades to the query-blind fallback (scope → equal seed timestamps → **ID ascending**), and the budget cut reproduces the original starvation shape for such queries.

**What Checkpoint A can claim:** Tier 4 recall works when the query shares literal vocabulary with the fact's key/domain/value (typical for direct gear/studio questions). **What it cannot claim:** synonym, paraphrase, or morphological-variant recall ("axe"→guitar, "tracking"→recording, "program"→DAW). That class waits on **KAI-789** (fact-vector index + embed-at-write; filed this review, below). A secondary honesty note is already in the code: the `excluded_stale` log field name is kept for compatibility but now counts lowest-ranked budget exclusions (`context_service.py:365-366` comment).

## C5 — BUDGET NOT GAMED — PASS

`TIER4_CHAR_CAP = 800 * 4` (`context_service.py:60`) appears in the `c34baf8` diff **only as unchanged context** — no budget change anywhere in the commit. Ranking is what changed. Live confirmation: reviewer packages still exclude under budget (excluded counts 15–19 across the six calls) — the fix selects better, it does not swallow all 26. ✔

## C6 — CONTAINMENT — PASS

- Full diff of `c34baf8` touches `context_service.py` (Tier 4 helpers + `_tier4_facts` + one call-site line) and the new test file only. **No** `registry.py` (write path untouched), **no** `router.py`, **no** endpoints. `assemble()`'s public signature unchanged at `:774`; `_tier4_facts` (internal) gained an optional `message: str = ""` parameter — the only call-site is `assemble()` at `:777`.
- **M1.5 `--global` regression reproduced:** the same global fact (`leo-bassman-layering-001`, `advisor:null`) surfaced first under BOTH roads (package `ec9874e5-d51b-483a-a6ab-8774d0b1ba61`) and sky (package `972a6c9d-4642-4744-b448-75229d69b319`) for the same query. ✔

## C7 — TESTS — PASS

- **Green-after:** full orchestrator suite in the deployed container: **15 passed, 1 skipped** (skip pre-existing).
- **Red-before (reviewer-run, mechanical):** full orchestrator tree copied in-container, only `context_service.py` swapped to `c34baf8~1` → focused test **fails** (`TypeError: _tier4_facts() takes 3 positional arguments but 4 were given` — the old reader had no query path at all).
- **Test genuinely encodes the property:** `test_tier4_relevance.py` builds 7 same-timestamp early fillers + late-positioned DAW/500-series/guitar facts under a tight monkeypatched cap (520 chars), then asserts (1) both late studio facts surface for a studio query, (2) the guitar fact surfaces for a guitar query, (3) the two result sets differ, (4) the same `advisor:null` facts read under both sky and roads. It tests the reader layer directly (`_tier4_facts`), not the endpoint, and cannot be satisfied by fixture reordering.
- **Lint:** in-image ruff still absent (KAI-675/776 — unchanged); host ruff on both changed files: **All checks passed!** Nothing new hides under the tracked gap.

## Deliverable — follow-up filed (not worked)

**KAI-789** `8d882352-bf04-48c5-a1e8-a9fa16b37283` — "Tier 4 semantic fact ranking — fact-vector index + embed-at-write (replaces lexical interim from KAI-788)". Backlog, medium, **parent KAI-788**, label `parked-post-gate`. Readback verified (state/parent/label). Description carries the documented C4 ceiling so the future builder inherits the truth-in-labeling.

## Reviewer hygiene

- Registry never written: SHA `195331d1…` byte-identical before and after all reviewer assembles (reads only).
- Container red-before residue (`/tmp/kai788-rb`, `/tmp/kai788-redbefore`) and host scratch removed.
- No engine close used anywhere (cb1c348c/D13).

## Discovered (no out-of-scope fixes)

- None new beyond the documented C4 ceiling (tracked as KAI-789). KAI-675/776 re-confirmed unchanged. The `excluded_stale` field-name mismatch is documented in-code and inherited by KAI-789's scope.

## Disposition

- KAI-788 → **Done** via manual API PATCH + readback.
- Checkpoint A may proceed with the C4 bounding language above.
