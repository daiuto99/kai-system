# Rule #9 review — STATUS.md reconciler

- Plane review ticket: `818c129e-aefb-4691-a113-8c613abba41a`
- Reviewed commit: `b948f01`
- Reviewer: Codex (independent of the Claude/Fable builder)
- Date: 2026-07-17
- Verdict: **FAIL**

## Scope

Independent live review of `KAI/scripts/status-watcher.sh`: periodic operation,
freshness protection, readback verification, file-descriptor handling, and the
failure alarm path.

## Remediated deployment finding

The current launchd job is not running the reconciler code under review.

```text
$ launchctl print gui/501/com.kai.status-watcher
state = running
active count = 1
pid = 47414
execs = 1
```

The job's current log contains only the old watcher protocol (for example,
`Change detected:` / `Syncing ...`) and no reconciler startup, heartbeat, or
`SYNCED` records. Its current stderr is repeatedly:

```text
/Users/leodaiuto/sonicink/kai/scripts/status-watcher.sh: line 39:
fswatch: command not found
```

The reviewed script has no `fswatch` reference; it is a new periodic loop with
an initial-pass heartbeat. This proves that the shell process started before the
replacement was written and has not been re-execed. A shell reads the script at
startup, so modifying the file did not change the code in the active process.

This deployment finding was remediated at 09:25. Independent follow-up evidence
showed launchd PID `63975`, a new reconciler startup record in
`~/.kai_status_reconcile/reconcile.log`, and matching Mac/worker SHA-256 hashes.

## Finding

Filed Plane `f661a05f-2758-4a9f-82ba-bfda5966fd28` — **[BUG][HIGH] STATUS
reconciler deployed source is not the live launchd process; old fswatch watcher
still runs**. This finding is now remediated.

## Resumed live test: freshness guard and alarm

I backed up the worker `KAI/STATUS.md`, appended a harmless review marker, and
set its mtime ten minutes into the future. A one-shot reconciler pass reported:

```text
ALARM: STATUS divergence: KAI/STATUS.md — worker vault copy is NEWER than the
git source and differs. NOT overwriting.
(no slack token at token file — alarm logged only)
initial pass complete: 7 STATUS.md checked, worker reachable
```

The target hash remained the tampered hash after the pass, proving the stale
overwrite guard. It was then restored from the preserved backup: worker and Mac
both returned SHA-256 `09afd5c...343d50d9`, the original mtime was restored, and
the temporary backup was removed.

The alarm acceptance criterion nevertheless fails. The configured token file
was unavailable to the live launchd process, so no `#devops` Slack notification
was sent. Filed Plane `24688e2-4d0d-4187-992e-3de6f3f2dc59` — **[BUG][HIGH]
STATUS reconciler alarm path is log-only: configured Slack token file is
unavailable**.

## Unverified because this blocking failure ends the review

- Live readback-mismatch (post-push) alarm test and Slack `#devops` delivery.
- A real `#devops` delivery after the credential-path defect is fixed.

No system behavior was modified by this review.
